server:
  port: 9001
spring:
  application:
    name: cloud-registry
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: false
    register-with-eureka: false
  server:
    enable-self-preservation: false
